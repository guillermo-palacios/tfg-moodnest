version: '3.8'
services:
  mongodb:
    image: mongo:latest
    container_name: moodnest_db
    ports:
      - "27017:27017"
    volumes:
      - moodnest_data:/data/db
    restart: unless-stopped

volumes:
  moodnest_data: